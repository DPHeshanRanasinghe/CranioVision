[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    slow: tests that run actual GPU inference (use -m "not slow" to skip)

# Quieter output
addopts =
    -v
    --tb=short
    --strict-markers